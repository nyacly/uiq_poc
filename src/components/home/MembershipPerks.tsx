// This component has been temporarily disabled because its data source,
// `src/lib/db/subscription-plans.ts`, was removed to fix build errors.
// This component will be re-implemented once the underlying data is available.
export function MembershipPerks() {
  return null
}